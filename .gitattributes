# Normalize line endings to LF for everything Git considers text, regardless
# of the OS/editor that touched it -- keeps diffs clean and identical whether
# this repo is cloned on Windows, Mac, or Linux (e.g. an interviewer's
# machine or a CI runner).
* text=auto eol=lf

# Explicit for anything Git's own auto-detection could get wrong.
*.png binary
